// import { S3Client } from "@aws-sdk/client-s3";

// const s3Client = new S3Client({
//   region: process.env.AWS_REGION || 'your-default-region', // Provide a default region if env variable is not set
//   credentials: {
//     accessKeyId: process.env.AWS_ACCESS_KEY_ID || 'your-access-key-id', // Provide default or throw error if not set
//     secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY || 'your-secret-access-key', // Provide default or throw error if not set
//   },
// });


// export default s3Client;
